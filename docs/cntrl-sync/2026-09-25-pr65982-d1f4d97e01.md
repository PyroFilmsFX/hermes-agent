# W2 upstream sync: merge of PR #65982 head d1f4d97e01 into cntrl-hermes-worker (c0deb07a36)

Date: 2026-09-25. Branch: `sync/w2-2026-09-25` (worktree `.worktrees/sync-w2`). Merge: `git merge --no-ff origin/pr-65982`.

## Method

- Topology (verified): `bb0c230366` (upstream main 2026-09-16) -> 41 old PR #65982 commits -> `6c580ca109`
  (old PR tip; same 41 subjects as the new PR head, in the same order) -> 148 `c0deb07*` carries + 2 carry merges -> `c0deb07a36`.
  New PR head `d1f4d97e01` = the same 41 commits rebased onto upstream main `0b724b178e`.
- A naive 3-way merge on the real merge-base `bb0c230366` produced **120 conflicted paths** (79 content, 40 add/add,
  1 modify/delete), because the old and new PR versions both look like independent edits. It also silently
  DUPLICATED code where the rebased PR moved blocks (verified: `tests/hermes_cli/test_models.py` got
  `test_claude_agent_sdk_pin_not_overridden_by_static_catalog` and 4 siblings twice).
- Resolution therefore used the old PR tip as the merge base:
  `git merge-tree --write-tree --merge-base=6c580ca109 HEAD origin/pr-65982` (diff3 markers), loaded with
  `git read-tree -u --reset` while MERGE_HEAD stayed in place. This is exactly "take the PR head's structure and
  re-apply our carry hunks": base->ours is the carry delta only, base->theirs is upstream 09-16..09-24 + the PR
  revision. Result: **51 content conflicts (83 hunks)**, **zero** in the PR-owned SDK facades/siblings/tests
  (all carry hunks on `agent/transports/claude_agent_sdk_session*`, `agent/claude_sdk_runtime*` and the split
  test modules applied cleanly onto the new PR structure; the fork already had the split layout).
- The naive merge's 69 conflicts that vanished under the better base were superseded-PR-version conflicts
  (old PR vs new PR text), not carry conflicts; they resolve to the PR head + carry delta automatically.
- The only clean-in-naive file whose content differs from the chosen result is `tests/hermes_cli/test_models.py`
  (the duplication above); the chosen result keeps one copy.
- rerere: the naive merge's preimages were cleared (`git rerere clear`) so rerere does not learn resolutions
  keyed to superseded-base conflicts.

## Summary

- Conflicted paths under the chosen base: **51** (83 hunks). By area: `apps/desktop` 21, `tui_gateway` 9, `tests` 6,
  root files 5 (`.gitignore`, `hermes_state.py`, `package.json`, `pyproject.toml`, `uv.lock`), `tools` 3, `agent` 3,
  `hermes_cli` 2, `apps/shared` (generated) 2. PR-owned SDK files: 0.
- Carry hunks dropped: **1**, intentional: the `provider.claude_agent_sdk` pin in `tools/lazy_deps.py` (`LAZY_DEPS`
  was deleted upstream). One redundant carry assertion in `test_anthropic_picker_curated.py` was replaced by a
  stronger upstream one. Nothing else dropped.
- Preservation spot-check of all 148 `c0deb07*` carries: 0 missing (details in the host's `/tmp/w2-preservation.md`
  at sync time; 127 fully present, 20 present with lines deliberately rewritten in the resolutions below, 1 with the
  lazy_deps hunk dropped).

## Conflicted paths (base 6c580ca109), resolution, carries re-applied

| Path | Hunks | Resolution | Carry hunks re-applied (commits) | Dropped |
|---|---|---|---|---|
| `.gitignore` | 1 | union: ours (cntrl/conductor ignores) then upstream (build scratch, PM lock, desktop dist) | c0deb07162, c0deb07282, c0deb07edb, c0deb07e98, c0deb07e81, c0deb074e5 | none |
| `agent/context_compressor.py` | 1 | both: upstream `_raise_if_stale_attempt` first (stale-at-entry guard), then our SDK display-only row filter | c0deb07ddb / c0deb072e2 (SDK display rows kept out of compression) | none |
| `agent/models_dev.py` | 2 | upstream's new `config=` signature and `unknown_base`, with our `_capability_catalog_identity` alias (claude-agent-sdk -> anthropic) threaded through `_get_provider_models`/`_find_model_entry`/`_builtin_model_metadata`/`_apply_overrides(config=...)`/`_explicit_model_override(config=...)` | c0deb076c2, c0deb07551 (SDK vision capability lookup) | none |
| `agent/title_generator.py` | 2 | our filler-stripping `derive_title` kept, given upstream's `title_preview` param and `build_title_input(...)` input; `_first_line` kept once (our position); upstream's `_extract_json_title`/`_is_truncated_structured_output` refactor taken; our unterminated-fence strip re-homed into upstream's new `_extract_title_text` | c0deb07dfb (short fallback titles), c0deb0769f (truncated reply not a title), c0deb07599 (attachment titles) | none |
| `package.json` | 1 | our `"eslint": "10.8.1"` devDependency kept; upstream moved `typescript-eslint` to the end (taken) | c0deb07084f | none |
| `pyproject.toml` | 1 | our `claude-agent-sdk==0.2.152` pin with upstream's comment spacing | c0deb07c27 (bump to 0.2.152) | none |
| `hermes_state.py` | 2 | both mixins: our `SessionPeerMailboxMixin` import + base-class entry alongside upstream's new `SessionProfileRepairMixin` | c0deb07690 (durable peer mailbox) | none |
| `tools/lazy_deps.py` | 1 (737-line) | upstream: the module is now a relaunch shim; the `LAZY_DEPS` table no longer exists upstream | c0deb07c27, c0deb0750e (SDK pin bump 0.2.150/0.2.152 in `LAZY_DEPS`) | **Intentionally dropped**: the `provider.claude_agent_sdk` lazy pin bump; the table it edited was deleted upstream. The 0.2.152 pin survives in the `pyproject.toml` extra. `tests/tools/test_lazy_deps.py` is deleted upstream and no carry touched it (the naive modify/delete conflict was an old-PR-vs-upstream artifact). |
| `tests/conftest.py` | 2 (h1 306-line) | upstream moved the env-filter tables into `tests/_fixtures/env_filter.py` (taken); our `_CLAUDE_CHILD_VARS` tuple re-homed directly under the conftest imports (its only consumer, `_hermetic_environment` step 2b, stayed in conftest and auto-merged); h2 keeps both autouse fixtures (our `_reset_claude_sdk_shutdown_fence`, upstream `_reset_foreground_exit_fence`). Diff vs PR head = +29 lines, carry only | c0deb0700f, c0deb07bbe | none |
| `tui_gateway/methods_prompt.py` | 4 | upstream's `_run_after_agent_ready(..., display_kind, display_metadata, hosted_terminal_callback, turn_author=None)` positional order taken (upstream test `test_submit_time_user_row` calls it positionally); `prompt.submit` keeps our `_submit_display(params)` gate (in-process `peer_message` kind + metadata) and merges upstream's `title_preview` into `display_metadata` | c0deb0702d, c0deb077e4 (peer mailbox display kind/metadata) | none |
| `tui_gateway/methods_session.py` | 3 | upstream hoisted `profile_home` above (taken); our `_create_overrides(params, profile_home=...)` kept; `_history_to_messages(history, profile_home=...)` (upstream) plus our `resolved_model`/`resolved_provider` computed inside `_profile_build_scope(profile_home)` (same intent as upstream's `_session_default_model`, and provider always reported) | c0deb078ba (W1 SDK default engine), c0deb07599 | none |
| `tui_gateway/methods_complete.py` | 3 | upstream's `_session_home_scope(...)` binding + `skill_bundles` snapshot, with our `merged_skill_commands(...)` (Claude Code plugin skills on the SDK lane) inside the scope | c0deb0777a | none |
| `tui_gateway/methods_tools.py` | 3 | upstream extracted `_refresh_live_sessions()`; our `sdk_rotate=True` re-homed as a new `sdk_rotate` kwarg on `_refresh_live_sessions` passed through to `refresh_agent_mcp_tools`, set only by `/reload-mcp` (plugin-install refresh keeps the default False); skills catalog: our `merged_skill_commands` + `origin: plugin` rows on top of upstream's `sc` alias and collision-note return | c0deb0777a, c0deb07add | none |
| `tui_gateway/methods_subagents.py` | 1 | upstream `_visible_subagent_records` with our per-record `kind` + `_enriched_meta` snapshot | c0deb07402, c0deb07093 | none |
| `tui_gateway/tool_progress.py` | 2 | upstream `_prepare_tool_result_metadata` + prepared-metadata consumption taken; our SDK kwargs on `_on_tool_complete` (`is_error`, `error`, `tool_use_result`, `truncated`) and SDK `is_error`/`error` payload fields kept, followed by upstream `labels` + metadata merge (the snapshot/diff render moved into `_prepare_...` upstream) | c0deb079a7, c0deb0752a, c0deb07187, c0deb07096 | none |
| `tui_gateway/session_lifecycle.py` | 1 | our shared-runtime-url lease metadata dict kept, now seeded from upstream's `_lease_metadata(live_session_id)` | c0deb07e46 | none |
| `tui_gateway/session_compression.py` | 1 | upstream session-context binding + `task_id`, called with our display-row-filtered `history` | c0deb07e16, c0deb072e2 | none |
| `tools/process_registry.py` | 1 | one `__post_init__`: upstream owner_task_id default, then our `generation`/`sdk_generation` alias sync | c0deb07493, c0deb07c58 | none |
| `tools/kanban_tools.py` | 1 | upstream `_persisted_session_id(...)` wrapping taken; our engaged-context gate (no os.environ fallback once a session context is engaged) re-applied on the ambient id, now read via upstream's `get_session_env` | c0deb07894 | none |
| `tests/tools/test_kanban_provenance.py` | 1 | both tests; our carry test now persists its two session ids in state.db because upstream stamps only persisted ids | c0deb07894 | none |
| `tui_gateway/contracts/events.py` | 1 | both: our SDK background-lane fields (`background`, `display_kind`, `display_metadata`, `delivery_id`) and upstream `persisted_turn` on `MessageCompletePayload` | c0deb07302, c0deb07492, c0deb077e4 | none |
| `hermes_cli/web_server.py` | 2 | both, in lifespan-exit order: our `begin_sdk_shutdown()` fence first, then upstream `clear_tui_message_injector()`; our `reap_sdk_children()` then upstream `eager_reconcile_thread.join()` | c0deb07bbe (fence + reap), c0deb07a36 | none |
| `hermes_cli/web_routers/sessions.py` | 1 | upstream made rename async (`asyncio.to_thread(_with_db, ...)`); our live-SDK-session rename push kept and also run via `asyncio.to_thread` (it ran in the sync endpoint's threadpool before) | c0deb07599 | none |
| `tests/hermes_cli/test_anthropic_picker_curated.py` | 3 | h1/h3: our carry versions kept (they pin the fork fix that the native Anthropic list carries `claude-fable-5-1`, the API id); upstream deleted those two tests in its low-value purge `d09dacf66a`. h2: upstream's stronger curated-prefix assertion taken (subsumes ours) | c0deb07a42, c0deb07136 | h2 ours superseded by a strictly stronger upstream assertion |
| `tests/agent/transports/test_hermes_tools_mcp_server.py` | 1 | ours (4 session-spawn exposure tests) with upstream's blank-line normalization | c0deb07c10, c0deb07e46, c0deb07a29 | none |
| `tests/agent/test_image_routing.py` | 1 | both classes (our `TestClaudeAgentSdkVisionRouting`, upstream `TestCodexContextVariantVisionLookup`) | c0deb076c2, c0deb07551 | none |
| `tests/gateway/test_unknown_command.py` | 1 | both (our 6 SDK-lane slash tests, upstream off-loop plugin command test) | c0deb07b6e2, c0deb0777a | none |
| `uv.lock` | 1 | upstream's lock (new `python_full_version` markers, cryptography 50.0.1) with only our `claude-agent-sdk==0.2.152` specifier re-added; the auto-merged package block carries our 0.2.152 wheels. Final diff vs PR head: 8 lines, all `claude-agent-sdk` 0.2.144 -> 0.2.152. No `uv lock`/`uv sync` was run | c0deb07c27, c0deb0750e | none |
| `apps/shared/src/gateway-contract.generated.ts` | 2 | **regenerated**, not hand-merged: reset to PR head, then `scripts/gen_gateway_contracts.py` with the live venv interpreter (read-only use) on the resolved `tui_gateway/contracts`; `--check` exits 0. Diff vs PR head is carry-only (peer mailbox payloads, SDK background-lane fields, `MessageStartPayload`, subagent tail doc) | c0deb07aa4, c0deb07492, c0deb07826, c0deb07ffa, c0deb077e4, c0deb07690, c0deb07402, c0deb074e5, c0deb078d70 | none |
| `apps/shared/src/gateway-contract.openrpc.json` | 1 | **regenerated** (same run) | same as above | none |
| `apps/desktop/electron/preload.ts` | 1 | both import groups (our `api-error-envelope`, upstream profile-route/HUD/window-controls types) | c0deb0742c | none |
| `apps/desktop/electron/main.ts` | 3 | upstream's `ActiveRuntimeState` import, `formatApiRequestFailure` and its try/catch + `dispatchConnectionScopedProfileDelete` body taken, wrapped in our `withQuietApiFailures(...)` envelope; the upstream catch now skips `rememberLog` for `isQuietApiFailure` errors (polled 404s) so the carry's quiet-log intent holds for desktop.log too | c0deb0742c | none (small adaptation: quiet-failure guard on the new upstream log line) |
| `apps/desktop/src/app/chat/index.tsx` | 1 | upstream dropped the unused `Button`/`ErrorState` imports; our `SessionMailboxPopover` import kept | c0deb07826, c0deb075ca | none |
| `apps/desktop/src/app/chat/sidebar/projects/workspace-groups.test.ts` | 1 | both named imports | c0deb0784f, c0deb0757a | none |
| `apps/desktop/src/app/chat/sidebar/projects/entered-content.tsx` | 2 | upstream `HomeSessions` + `PROJECT_SESSION_PAGE`/`useRevealedRows` taken; our `ConductorLaneRollup` import and `export` on `RepoFlatSection` (imported by our `lane-rollup.test.tsx`) re-applied | c0deb0784f, c0deb0757a | none |
| `apps/desktop/src/contrib/plugin.ts` | 1 | both imports (upstream `./events`, ours `./react/surface`) | c0deb0741b | none |
| `apps/desktop/src/styles.css` | 1 | upstream's 80% composer fill with our `backdrop-filter: blur(0.75rem)` lines re-applied | c0deb079df8 | none |
| `apps/desktop/src/app/contrib/wiring.tsx` | 2 | upstream: the post-turn rehydrate loop moved to `hydrateStoredSessionTranscript` in `use-background-sync.ts` (taken; the now-unused todos import dropped with it) | c0deb07302 | none: our `sdk_tasks` todo-source guard re-homed (next row) |
| `apps/desktop/src/app/contrib/hooks/use-background-sync.ts` | 1 | both: our `goneTileSignatures` + `storedRowProfileScope` and upstream's new `hydrateStoredSessionTranscript`; our `$todoSourcesBySession[...] === 'sdk_tasks'` early return (from wiring.tsx) re-homed before its todo restore, `$todoSourcesBySession` import added | c0deb0739f, c0deb078ba, c0deb07168, c0deb07302 | none |
| `apps/desktop/src/types/hermes.ts` | 1 | both union members (our peer-message / lifecycle metadata shapes, upstream `tool_result_metadata`) | c0deb07302, c0deb072e2 | none |
| `apps/desktop/src/app/types.ts` | 1 | both fields (our `sealedInterimId`, upstream `heartbeatSettledStreamId`) | c0deb073dd, c0deb07a89 | none |
| `apps/desktop/src/app/session/hooks/use-message-stream/index.ts` | 7 | upstream's `eventTarget`/`patchesSealedBubble` reconciliation, `heartbeatSettledStreamId`, `persistedTurn`/`responseTransformed` params, `withPersistedIdentity`, `recovered: false`, and the occurrence-bounded `streamIndex` + `collapseDuplicateFinalAfterToolInterim` taken; our SDK background-lane `deliveryId` plumbing re-applied: stamped on streamed bubbles, `deliveryId` appended as the LAST param of `completeAssistantMessage`, and a `deliveryIndex` fallback (`else if (deliveryIndex >= 0) settleAt(...)`) that settles a background completion onto the bubble carrying its delivery id before upstream's generic fallback | c0deb073dd, c0deb07a89, c0deb072e2 | none (our `targetIndex = streamIndex ?? deliveryIndex` became an explicit `else if` branch inside upstream's structure) |
| `apps/desktop/src/app/session/hooks/use-message-stream/gateway-event/message-stream.ts` | 2 | our background-lane `message.start` branch kept; its unconditional `flushQueuedDeltas` replaced by upstream's turn-boundary flush-or-drop (#119543), placed before the background branch so both lanes follow it; `completeAssistantMessage(... persisted_turn, response_transformed, delivery_id)` | c0deb07376a, c0deb0718c, c0deb07826, c0deb07bca, c0deb075ca, c0deb073dd, c0deb07610, c0deb07a89, c0deb07084f, c0deb072e2 | none |
| `apps/desktop/src/app/session/hooks/use-message-stream/gateway-event/types.ts` | 1 | signature: upstream's `persistedTurn`, `responseTransformed`, then our `deliveryId` | c0deb073dd | none |
| `apps/desktop/src/app/session/hooks/use-session-actions/index.ts` | 1 | upstream's `includeComposerSelection` gate taken; our W1 rule (send `model` only together with `provider`) re-applied inside it | c0deb078ba | none |
| `apps/desktop/src/app/session/hooks/use-session-actions/resolve-stored-session.test.ts` | 1 | union of imports (`$unlistedSessionOwnerRows` upstream, `session-gone-latch` ours) | c0deb078ba, c0deb07f26 | none |
| `apps/desktop/src/app/settings/config-settings.tsx` | 1 | union: our `dataUpdatedAt: configUpdatedAt` plus upstream `writeScope` (the hook returns the query object, so both exist) | c0deb07ad5 | none |
| `apps/desktop/src/app/shell/model-catalog-menu.test.tsx` | 1 | both describe blocks | c0deb078ba | none |
| `apps/desktop/src/components/assistant-ui/tool/fallback-model/index.ts` | 1 | upstream's interrupted-vs-unavailable label, with our ` · <tool title>` suffix re-applied | c0deb07420, c0deb07096 | none |
| `apps/desktop/src/components/assistant-ui/tool/fallback.tsx` | 2 | upstream `summaryOnly`/`showInlineDiff`/`showPayload` gating taken; our `view.truncatedLabel` expandable trigger and truncation note re-applied | c0deb07096 | none |
| `apps/desktop/src/lib/chat-messages/hydration.ts` | 3 | our `effectiveDisplayKind` (peer_message/session_lifecycle as system rows) plus upstream `failed_turn`; our `deliveryId` extraction + `isDisplayAssistant` branch with upstream `pendingAbsorbedRows`; both `durableComplete` (upstream) and `deliveryId` (ours) stamped on the active assistant | c0deb0718c, c0deb07171, c0deb074c1, c0deb079f4, c0deb072be, c0deb07dcf, c0deb07826, c0deb075ca, c0deb073dd, c0deb072e2 | none |

## Post-resolution merge fallout (non-conflicted files, found by type-check)

| Path | Fix | Carry | Why |
|---|---|---|---|
| `apps/desktop/src/app/chat/composer/composer-blur.test.tsx` | removed the two `onAttachPrCommentUrl={vi.fn()}` props | c0deb079df8 (our test) | upstream removed `onAttachPrCommentUrl` from `ChatBarProps` (see upstream `paste-url-is-text.test.tsx`); TS2322 |
| `apps/desktop/src/app/contrib/onboarding-kickoff.test.ts` | added `sealedInterimId: null` to the literal `ClientSessionState` | c0deb073dd / c0deb07084f (our required field) | new upstream test builds a full `ClientSessionState`; TS2741 |

## Checked, no change needed

- `hermes_cli/config_defaults.py` auto-merged; fork default flips verified present in the merged file:
  `permission_mode: "auto"`, `session_name: "hermes:{title}"`, `streaming: True`, `task_tools: True`,
  `deliver_background_results: False` (same as the fork tip), plus `peer_mailbox`, `session_spawn`, `session_send`, `cli_path`, `plugins`.
- `agent/transports/claude_agent_sdk_session*`, `agent/claude_sdk_runtime*`, and the SDK split test modules: no conflicts under
  the old-PR-tip base; carry hunks applied onto the PR head's split layout automatically.

## Known pre-existing / environmental type errors (not introduced by this merge)

- `apps/desktop/src/lib/chat-messages/hydration.test.ts` lines 98 and 461: `from_name` / `delivery_id` are not in
  `TimelineDisplayMetadata`. Identical on the fork tip `c0deb07a36` (the type never declared them; the tests are carry tests).
- `apps/desktop/src/components/onboarding-chat/cards/setup.tsx`: `lucide-react` unresolved. Upstream file; `apps/desktop/package.json`
  does not declare the dependency (upstream relies on hoisting). Type-check ran against the live checkout's node_modules (temporary symlink,
  removed after), which predate the 09-24 dependency set, so electron `Cannot find module` errors (yaml, ws, dbus-native, electron-updater, ...)
  are environmental. None are in `electron/main.ts` or `electron/preload.ts`.

## Verification at merge time

- `py_compile` of every `.py` file that differs from the PR head (153 files): OK.
- Smoke import from the worktree root, `PYTHONPATH=.`, live venv interpreter used read-only:
  `import agent.transports.claude_agent_sdk_session, agent.claude_sdk_runtime, tui_gateway.server, hermes_cli.web_server, gateway.run` -> OK
  (module resolved from the worktree, not the live checkout).
- `env -u HERMES_HOME HERMES_PYTHON=<live venv python> scripts/run_tests.sh -j 4 <44 files>`: **1183 passed, 0 failed**.
  Files: all 31 `tests/agent/test_claude_sdk_*.py` (includes the 15 PR split modules), `tests/agent/transports/test_claude_sdk_session_env_leak.py`,
  `tests/tui_gateway/test_session_mailbox.py`, `tests/tui_gateway/test_session_spawn_integration.py`,
  `tests/agent/transports/test_hermes_gateway_session_bridge.py`, `tests/gateway/test_bots_allowlist_gate.py`, plus the hand-resolved
  test modules (`test_kanban_provenance`, `test_anthropic_picker_curated`, `test_image_routing`, `test_unknown_command`,
  `test_hermes_tools_mcp_server`, `test_models`, `test_submit_time_user_row`, `test_packaging_metadata`).
- `scripts/gen_gateway_contracts.py --check`: exit 0.
- Desktop `tsc --noEmit` (renderer + electron) against borrowed node_modules: only the pre-existing/environmental errors listed above.
- Not run here (host runs later): the full Python suite, desktop vitest, eslint.
